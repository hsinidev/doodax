
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add other Next.js configurations here as the project evolves.
  // For example, image optimization domains, redirects, etc.
};

module.exports = nextConfig;
